streamlit>=1.38
pandas>=2.2
pyarrow>=17.0
requests>=2.32
plotly>=5.24
boto3>=1.34
openpyxl>=3.1
fredapi>=0.5
yfinance>=0.2

# Rebuild marker: bump this line to force Streamlit Cloud to reinstall the
# environment and start a clean deploy (clears stale-build state without a
# manual reboot). Last: CB-dashboard brief — engine, methodology + daily-signals tabs, snapshots.
